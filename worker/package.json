{
  "name": "imgbase-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply IMGBASE_DB --local",
    "test:integration": "npm run migrate:local && vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.5.12",
    "@cloudflare/workers-types": "^4.20251004.0",
    "typescript": "5.4.5",
    "vitest": "2.1.1",
    "wrangler": "^4.42.0"
  },
  "dependencies": {
    "itty-router": "3.0.12"
  }
}
